===https://www.codewars.com/kata/56d93f249c844788bc000002===
```function testit(s){
  return s.split(' ')
    .map(str => 
      str.substring(0, str.length - 1) + str.substring(str.length - 1).toUpperCase()
    )
    .join(' ');
}
```
